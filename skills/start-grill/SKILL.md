---
name: start-grill
description: Legacy alias for grill-with-docs. Use only when start-grill is invoked explicitly.
disable-model-invocation: true
---

# Start Grill

This is a compatibility alias for `/grill-with-docs`.

For normal plan or design sharpening, run `/grill-with-docs`: `/grilling` with `/domain-modeling` active.

Do not estimate context percentages. Do not route by size.

Use `/grill-prep` only when the user explicitly asks for persistent multi-session topic docs.

## Done

Done means the request has been handed to `/grill-with-docs` or, on explicit request, `/grill-prep`.
